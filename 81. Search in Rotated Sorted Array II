class Solution:
    def search(self, nums: List[int], target: int) -> bool:
        if not nums:
            return False
        #duplicates cannot use binary search
        for n in nums:
            if n==target:
                return True
        return False
